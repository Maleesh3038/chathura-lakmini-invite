export const metadata = {
  title: 'Wedding Wishes — Display',
  robots: {
    index: false,
    follow: false,
  },
};

export default function WishesDisplayLayout({ children }) {
  return children;
}
