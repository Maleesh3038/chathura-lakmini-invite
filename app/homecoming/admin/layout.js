export const metadata = {
  title: 'Home Coming Dashboard — Admin',
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function HomecomingAdminLayout({ children }) {
  return children;
}
